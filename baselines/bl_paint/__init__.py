"""bl_paint: the fused arm of the section 6.2 ablation, and the naive
early-fusion floor of problem statement section 7.

Deliberately empty of imports, like the other baseline packages. The class
lives in baseline.py and the CLI in run.py, so eval/sweep.py can import the
one it needs without paying for the other.
"""
